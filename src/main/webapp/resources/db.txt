CREATE SCHEMA `my_project` ;

CREATE TABLE `my_project`.`tasks` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(150) NULL,
  `type` VARCHAR(50) NULL,
  `description` VARCHAR(1000) NULL,
  `topic` VARCHAR(50) NULL,
  PRIMARY KEY (`id`));


INSERT INTO `my_project`.`tasks`
(`id`,
`name`,
`type`,
`description`,
`topic`)
VALUES
('1',
'Enum',
'PRACTICE_TASK',
'Create enam for marital status with the ability to find an instance of id',
'if statement');